providers = []

[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.build]
cmds = ["cd backend && mvn clean package -DskipTests -q"]

[start]
cmd = "java -jar backend/target/ai-chat-backend-1.0.0.jar"
